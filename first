CREATE TABLE store (id INTEGER PRIMARY KEY, item TEXT, price INTEGER, asile INTEGER, in_stock TEXT, color TEXT);
INSERT INTO store VALUES (1, "dresses", 5.00, 1, "yes", "red");
INSERT INTO store VALUES (2, "shoes", 2.00, 2, "no", "blue");
SELECT *FROM store WHERE asile > 1
